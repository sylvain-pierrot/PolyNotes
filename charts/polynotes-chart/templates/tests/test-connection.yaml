apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "polynotes-chart.fullname" . }}-test-connection"
  labels:
    {{- include "polynotes-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "polynotes-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
