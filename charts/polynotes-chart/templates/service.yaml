apiVersion: v1
kind: Service
metadata:
  name: {{ include "polynotes-chart.fullname" . }}
  labels:
    {{- include "polynotes-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "polynotes-chart.selectorLabels" . | nindent 4 }}
